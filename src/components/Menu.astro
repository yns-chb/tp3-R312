<nav
    class="flex flex-wrap items-center gap-3 border-b border-slate-200 bg-white px-6 py-4 shadow-sm"
    aria-label="Navigation principale"
>
    <a
        href="/"
        class="font-semibold text-slate-700 hover:text-cyan-700 focus-visible:outline-2 focus-visible:outline-cyan-700"
        >Accueil</a
    >

    <span class="text-sm font-bold uppercase tracking-wide text-cyan-700"
        >Penguins</span
    >
    <a
        href="/Penguins/slider/"
        class="text-slate-600 hover:text-cyan-700 focus-visible:outline-2 focus-visible:outline-cyan-700"
        >Slider</a
    >
    <a
        href="/Penguins/onglet/"
        class="text-slate-600 hover:text-cyan-700 focus-visible:outline-2 focus-visible:outline-cyan-700"
        >Onglets</a
    >

    <span class="text-sm font-bold uppercase tracking-wide text-amber-700"
        >Cars</span
    >
    <a
        href="/Cars/slider/"
        class="text-slate-600 hover:text-amber-700 focus-visible:outline-2 focus-visible:outline-amber-700"
        >Slider</a
    >
    <a
        href="/Cars/onglet/"
        class="text-slate-600 hover:text-amber-700 focus-visible:outline-2 focus-visible:outline-amber-700"
        >Onglets</a
    >

    <span class="text-sm font-bold uppercase tracking-wide text-orange-700"
        >Pizza</span
    >
    <a
        href="/Pizza/slider/"
        class="text-slate-600 hover:text-orange-700 focus-visible:outline-2 focus-visible:outline-orange-700"
        >Slider</a
    >
    <a
        href="/Pizza/onglet/"
        class="text-slate-600 hover:text-orange-700 focus-visible:outline-2 focus-visible:outline-orange-700"
        >Onglets</a
    >
</nav>

<div
    class="flex flex-wrap items-center gap-3 border-b border-slate-200 bg-slate-50 px-6 py-3"
>
    <label for="species" class="font-medium text-slate-700">Espèce</label>
    <select
        id="species"
        class="rounded border border-slate-300 bg-white px-3 py-2"
    >
        <option value="Adelie">Adelie</option>
        <option value="Chinstrap">Chinstrap</option>
        <option value="Gentoo">Gentoo</option>
    </select>

    <label for="critere" class="font-medium text-slate-700">Critère</label>
    <select
        id="critere"
        class="rounded border border-slate-300 bg-white px-3 py-2"
    >
        <option value="Moteur">Moteur</option>
        <option value="Performance">Performance</option>
        <option value="Poids">Poids</option>
    </select>

    <label for="categorie" class="font-medium text-slate-700">Pizza</label>
    <select
        id="categorie"
        class="rounded border border-slate-300 bg-white px-3 py-2"
    >
        <option value="Classic">Classic</option>
        <option value="Speciality">Speciality</option>
        <option value="Vegetarian">Vegetarian</option>
    </select>
</div>

<script>
    document.querySelector("#species")?.addEventListener("change", (evt) => {
        const value = (evt.target as HTMLSelectElement).value;
        location.pathname = "/Penguins/species/" + value + "/";
    });
</script>

<select id="critere">
    <option value="Moteur">Moteur</option>
    <option value="Performance">Performance</option>
    <option value="Poids">Poids</option>
</select>
<script>
    document.querySelector("#critere")?.addEventListener("change", (evt) => {
        const value = (evt.target as HTMLSelectElement).value;
        location.pathname = "/Cars/critere/" + value + "/";
    });
</script>

<script>
    document.querySelector("#categorie")?.addEventListener("change", (evt) => {
        const value = (evt.target as HTMLSelectElement).value;
        location.pathname = "/Pizza/category/" + value + "/";
    });
</script>
